{
  "name": "madz",
  "version": "1.0.0",
  "description": "A personality-driven AI harness channeling Mads Mikkelsen's cinematic soul.",
  "keywords": [
    "cli",
    "ai",
    "engine",
    "assistant",
    "harness",
    "orchestrator",
    "orchestrator-engine"
  ],
  "homepage": "https://github.com/avoidwork/madz#readme",
  "bugs": {
    "url": "https://github.com/avoidwork/madz/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avoidwork/madz.git"
  },
  "license": "BSD-3-Clause",
  "author": "Jason Mulligan <jason.mulligan@avoidwork.com>",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "bun index.js --mode interactive",
    "test": "node --test $(find tests -name '*.test.js' ! -name 'tui.test.js' ! -name 'conversationPanel.test.js' ! -name 'memoryLimit.test.js')",
    "coverage": "node --test --experimental-test-coverage --test-coverage-exclude=dist/** --test-coverage-exclude=tests/** --test-coverage-exclude=src/tui/** --test-reporter=spec $(find tests -name '*.test.js' ! -name 'tui.test.js' ! -name 'conversationPanel.test.js' ! -name 'memoryLimit.test.js') 2>&1 | grep -A 1000 'start of coverage report' > coverage.txt",
    "fix": "oxlint --fix *.js src tests/unit && oxfmt *.js src tests/unit --write",
    "lint": "oxlint *.js src tests/unit && oxfmt *.js src/*.js tests/unit/*.js --check",
    "prepare": "husky",
    "purge-branches": "git branch | grep -v \"main\" | xargs git branch -D"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "oxfmt": "^0.53.0",
    "oxlint": "^1.68.0"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.3.5",
    "@langchain/langgraph-checkpoint-sqlite": "^1.0.1",
    "@langchain/openai": "^1.4.7",
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/sdk-node": "^0.218.0",
    "@opentui/core": "^0.3.2",
    "@opentui/react": "^0.3.2",
    "cron-parser": "^5.5.0",
    "js-yaml": "^4.2.0",
    "posix": "^4.2.0",
    "react": "^19.2.0",
    "zod": "^4.1.8"
  }
}
