providers:
  openai:
    type: openai
    base_url: https://api.openai.com/v1
    model: gpt-4o
    encoding: cl100k_base
    credentials:
      apiKey:
    temperature: 0.4
    maxTokens: 4096
    rateLimit:
      requestsPerMinute: 120
email:
  provider:
    type: gmail
    clientId:
    clientSecret:
    refreshToken:
    accessToken:
    refreshTokenUrl:
    tenantId:
    host:
    port:
    secure:
    user:
    password:
  defaultFolder: INBOX
  maxAttachments: 10
  maxAttachmentSize: 25mb
sandbox:
  paths:
    - "./"
    - "/tmp"
  skillScanPaths:
    - "skills/"
    - ".skills/"
  timeout:
    seconds: 600
    gracePeriod: 5
  memoryLimit: 128mb
  safety:
    urlFilter: true
    pythonImportHook: true
  env:
    allowlist:
      - PATH
      - HOME
      - NODE_ENV
      - OPENAI_API_KEY
      - AUTH_API_KEY
      - EMAIL_PROVIDER_TYPE
      - EMAIL_PROVIDER_CLIENT_ID
      - EMAIL_PROVIDER_CLIENT_SECRET
      - EMAIL_PROVIDER_REFRESH_TOKEN
      - EMAIL_PROVIDER_ACCESS_TOKEN
      - EMAIL_PROVIDER_REFRESH_TOKEN_URL
      - EMAIL_PROVIDER_TENANT_ID
      - EMAIL_PROVIDER_HOST
      - EMAIL_PROVIDER_PORT
      - EMAIL_PROVIDER_SECURE
      - EMAIL_PROVIDER_USER
      - EMAIL_PROVIDER_PASSWORD
      - EMAIL_DEFAULT_FOLDER
      - EMAIL_MAX_ATTACHMENTS
      - EMAIL_MAX_ATTACHMENT_SIZE
  permissions:
    - filesystem:read
    - filesystem:write
    - filesystem:exec
    - process:spawn
    - network:outbound
  maxReadSize: 10mb
memory:
  directory: memory/
  contextDir: memory/context/
  schedulesDir: memory/schedules/
  sessionsDir: memory/sessions/
  gc:
    enabled: true
    idleTimeoutMs: 300000
    maxGcPerHour: 4
telemetry:
  enabled: false
  exporter:
    protocol: console
    endpoint: http://localhost:4318
    batch:
      maxSize: 512
      scheduledDelay: 5000
  sampling:
    ratio: 0.1
  redact:
    paths:
      - credentials.apiKey
      - headers.authorization
      - request.headers.x-api-key
schedules:
  maxConcurrent: 1
  syncOnInit: true
  entries: []
tui:
  name: madz
  cursorChar: "█"
agent:
  recursionLimit: 1000
  autoContinueLimit: 1000
  nodeTimeout: 600000
  deepAgents:
    codingAgent:
      description: "Specialized agent for code-related tasks including file editing, debugging, and implementation."
      temperature: 0.3
lru:
  size: 100
  ttl: 600000
persistence:
  mode: memory
  sqlite_path: memory/checkpoints.db
cwd: ""
