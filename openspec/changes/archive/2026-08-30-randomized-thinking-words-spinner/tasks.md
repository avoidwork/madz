## 1. Add Thinking Words Constant

- [x] 1.1 Add THINKING_WORDS constant array at module scope in messageBubble.js with 25 curated words

## 2. Add Random Word Helper Function

- [x] 2.1 Add getRandomThinkingWord() helper function that returns a random word from THINKING_WORDS using Math.random()

## 3. Replace Hardcoded Text

- [x] 3.1 Replace the hardcoded " thinking" string in the JSX (line 278) with a call to getRandomThinkingWord()
- [x] 3.2 Ensure the word is displayed with a leading space before the word (e.g., " Brewing")

## 4. Verify Implementation

- [x] 4.1 Verify the app starts without errors (npm start with timeout)
- [x] 4.2 Verify the word changes on each render by checking the code logic
